
abstract class Player {
  /// Represents the player's `GameRole`
  String gameRole,

      /// Represents a `LobbyRole` constant
      lobbyRole,

      /// Represents a `PlayerPrivilege` constant
      privilege,

      /// The Player's display name
      displayName;
}
