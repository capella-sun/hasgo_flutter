/// Game Mode Enumeration
/// There should be a separate child directory for each game mode
class GameMode {
  // Using strings instead of constants for easier compatibility with web interfaces

  static const String HIDE_AND_SEEK = 'hide_and_seek';

}